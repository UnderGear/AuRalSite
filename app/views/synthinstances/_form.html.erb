<%= form_for(@synthinstance) do |f| %>
  <% if @synthinstance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@synthinstance.errors.count, "error") %> prohibited this synthinstance from being saved:</h2>

      <ul>
      <% @synthinstance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :params %><br />
    <%= f.text_area :params %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
