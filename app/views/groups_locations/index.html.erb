<h1>Listing groups_locations</h1>

<table>
  <tr>
    <th>Group</th>
    <th>Location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups_locations.each do |groups_location| %>
  <tr>
    <td><%= groups_location.group_id %></td>
    <td><%= groups_location.location_id %></td>
    <td><%= link_to 'Show', groups_location %></td>
    <td><%= link_to 'Edit', edit_groups_location_path(groups_location) %></td>
    <td><%= link_to 'Destroy', groups_location, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Groups location', new_groups_location_path %>
