<%= form_for(@locationsets_user) do |f| %>
  <% if @locationsets_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@locationsets_user.errors.count, "error") %> prohibited this locationsets_user from being saved:</h2>

      <ul>
      <% @locationsets_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :locationset_id %><br />
    <%= f.text_field :locationset_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
