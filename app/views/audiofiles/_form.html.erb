<%= form_for(@audiofile) do |f| %>
  <% if @audiofile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@audiofile.errors.count, "error") %> prohibited this audiofile from being saved:</h2>

      <ul>
      <% @audiofile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
