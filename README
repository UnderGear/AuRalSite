This is the Server side that goes along with the AuRal project.

It handles user interaction with parameters of locations that exist in the database, aggregates them, and sends them out to other users in that location via OSC.